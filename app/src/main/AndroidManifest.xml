<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pidois.ester">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Controller.ProfileActivity"
            android:theme="@style/ProfileTheme"></activity>
        <activity android:name=".Controller.ExercisesActivity"></activity>
        <activity android:name=".Controller.CognitiveActivity" />
        <activity
            android:name=".Controller.NewsActivity"
            android:theme="@style/MyAppBarLayoutTheme">
        </activity>
        <activity android:name=".Controller.NewsDetailActivity" />
        <activity android:name=".Controller.MainActivity" />
        <activity
            android:name=".Controller.LoginActivity"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".Controller.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>